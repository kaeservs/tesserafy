Python evaluation harness and labelling CLI. Not a runtime dependency of the
web app. Introduced in Phase 3; the labelled corpus starts accumulating in
Phase 0 because sourcing it is slower than building the harness.
