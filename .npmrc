# Hoist nothing by default: workspace packages must declare what they import.
# Catches the "it worked because a sibling installed it" class of bug early.
shamefully-hoist=false
strict-peer-dependencies=false
