{
  "name": "tesserafy",
  "version": "0.0.0",
  "private": true,
  "description": "Turn every conversation into product intelligence.",
  "license": "UNLICENSED",
  "packageManager": "pnpm@12.4.1",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "dev": "pnpm --filter @tesserafy/web dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "db:types": "echo 'see docs/engineering/database.md — generates packages/db/src/generated.ts'"
  }
}
