# Migrations

Applied through the Supabase MCP server or CLI. Never run ad-hoc SQL against
the remote project — every schema change lands here first.

Schema-qualify the vector type as `extensions.vector(768)`.
